name: build
on: 
  push: 
    branches: 
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2 # If you're using actions/checkout@v2 you must set persist-credentials to false in most cases for the deployment to work correctly.
        with:
          persist-credentials: false

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        run: |
          npm install yarn -g 
          yarn 
          yarn build 

      - name: createTag 🚀 
        uses: appleboy/ssh-action@master
        with:
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          host: ${{ secrets.SSH_HOST }}
          script: |
            rm -rf ${{TEMP_REMOTE_PATH}}
            mkdir -p ${{REMOTE_PATH}}
            mv ${{REMOTE_PATH}} ${{TEMP_REMOTE_PATH}}
            npm i pm2 -g
            npm i yarn -g

      # ------- scp the file ----------
      # dist 
      - name: Deploy dist 🍱
        uses: garygrossgarten/github-action-scp@release
        with:
          local: "./dist/"
          remote: ${{REMOTE_PATH}}/dist
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
      # .nuxt 
      - name: Deploy .nuxt 🍱
        uses: garygrossgarten/github-action-scp@release
        with:
          local: "./.nuxt/"
          remote: ${{REMOTE_PATH}}/.nuxt
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
      # package.json 
      - name: Deploy package.json 🍱
        uses: garygrossgarten/github-action-scp@release
        with:
          local: "./package.json"
          remote: ${{REMOTE_PATH}}/package.json
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
      # nuxt.config.js
      - name: Deploy nuxt.config.js 🍱
        uses: garygrossgarten/github-action-scp@release
        with:
          local: "./nuxt.config.js"
          remote: ${{REMOTE_PATH}}/nuxt.config.js
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}

      - name: start server 🚀 
        uses: appleboy/ssh-action@master
        with:
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          host: ${{ secrets.SSH_HOST }}
          script: |
            cd ${{REMOTE_PATH}}
            yarn -production
            pm2 start ${{REMOTE_PATH}}/node_modules/nuxt/bin/nuxt.js --name movie-vueapp

           
